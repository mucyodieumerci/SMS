open terminal(If it is possible switch the terminal to command prompt)
cd backend-project
npm run dev
open terminal(If it is possible switch the terminal to command prompt)
cd frontend-project
npm run dev
